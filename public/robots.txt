User-agent: *
Allow: /

# Don't index the admin panel — it's just a UI shell, content comes from the API.
Disallow: /admin
Disallow: /admin/

# AI assistants and their crawlers are explicitly welcome: the bulletins are
# meant to be read, quoted and shared (with credit to עלון משמעות + a link).
# Listed by name because several of these are blocked by default on some
# platforms — this states the opposite intent.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: Bingbot
User-agent: CCBot
User-agent: Amazonbot
User-agent: meta-externalagent
User-agent: cohere-ai
User-agent: YouBot
Allow: /
Disallow: /admin
Disallow: /admin/

# Machine-readable entry points (see /llms.txt for the full guide):
#   /llms.txt       — what this site is + where everything lives
#   /llms-full.txt  — the full text of every bulletin, in one file
#   /feed.xml       — RSS, full article text per item
#   /y/<year>/<slug>.txt   — a single bulletin as plain text
#   /data/bulletins/<year>/<slug>.json — a single bulletin as structured JSON

Sitemap: https://alonmashmaut.org/sitemap.xml
